---
const experiences = [
  {
    period: "Jul 2023 — Presente",
    company: "Epam Systems",
    role: "Full Stack Developer",
    description: [
      "Desarrollo y mantención de sistemas de recaudación de pagos con distintas pasarelas de pago para más de <span class='text-teal-400 font-semibold'>40.000 clientes mensuales</span>.",
      "Implementación de <span class='text-teal-400 font-semibold'>pipelines en Azure Data Factory</span> para el manejo eficiente de datos de diversas fuentes.",
      "Participación activa en el modelamiento de <span class='text-teal-400 font-semibold'>sistemas relacionales</span> mejorando la eficiencia de las consultas y la minimizando la inconsistencia de datos.",
      "Mentoría a desarrolladores para crear <span class='text-teal-400 font-semibold'>tests E2E</span> y realizar refactorings de código."
    ],
  },
  {
    period: "Sep 2021 — Jul 2023",
    company: "Bel-Ray Chile",
    role: "Full Stack Developer",
    description: [
      "Desarrollo de <span class='text-teal-400 font-semibold'>aplicación móvil offline</span> con sistema de sincronización para la gestión de mantención de equipos industriales.",
      "Configuración de despliegue en <span class='text-teal-400 font-semibold'>Google Cloud Run</span>, Firebase y <span class='text-teal-400 font-semibold'>Cloud Functions</span> para alta disponibilidad.",
      "Creación de <span class='text-teal-400 font-semibold'>workflows en GitHub Actions</span> para despliegue automatizado y notificaciones en Slack."
    ],
  },
]
---

<section id="experience" class="py-20 border-t border-zinc-800">
  <h2 class="text-sm font-medium text-teal-400 mb-12 uppercase tracking-wider fade-in">
    Experiencia
  </h2>

  <div class="space-y-16">
    {experiences.map((exp, index) => (
      <div class="group fade-in" style={`animation-delay: ${index * 100}ms`}>
        <div class="mb-4">
          <div class="flex items-start justify-between mb-4">
            <div class="flex-1">
              <div class="flex items-center gap-3 mb-2">
                <h3 class="text-xl font-medium group-hover:text-teal-400 transition-colors">{exp.role}</h3>
              </div>
              <p class="text-zinc-400 leading-relaxed mb-0">{exp.company}</p>
            </div>
            <div class="text-sm text-zinc-500 font-mono ml-8">
              {exp.period}
            </div>
          </div>
          <div class="space-y-3 mb-3">
            {exp.description.map((paragraph) => (
              <p class="text-zinc-400 leading-relaxed" set:html={paragraph}></p>
            ))}
          </div>
        </div>
      </div>
    ))}
  </div>
</section>
